<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sk.piskula.fuelup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".screens.VehicleList"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screens.VehicleTabbedDetail"
            android:label="Detail"
            android:launchMode="singleTop"
            android:parentActivityName=".screens.VehicleList"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="sk.piskula.fuelup.screens.VehicleList" />
        </activity>
        <activity
            android:name=".screens.edit.EditExpense"
            android:label="Expense"
            android:parentActivityName=".screens.VehicleTabbedDetail">
        </activity>
        <activity
            android:name=".screens.edit.AddVehicle"
            android:label="Add Vehicle"
            android:parentActivityName=".screens.VehicleList">
        </activity>
        <activity
            android:name=".screens.edit.EditVehicle"
            android:label="Edit Vehicle"
            android:parentActivityName=".screens.VehicleTabbedDetail">
        </activity>
    </application>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

</manifest>